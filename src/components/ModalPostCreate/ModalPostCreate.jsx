import { Modal } from "../../Modal/Modal";
import "./ModalPostCreate";

export function ModalPostCreate({ modalState, setModalState }) {
  //LOGICA
  return <Modal modalState={modalState} setModalState={setModalState}></Modal>;
}
